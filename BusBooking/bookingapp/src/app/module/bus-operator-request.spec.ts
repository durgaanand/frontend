import { BusOperatorRequest } from './bus-operator-request';

describe('BusOperatorRequest', () => {
  it('should create an instance', () => {
    expect(new BusOperatorRequest()).toBeTruthy();
  });
});
